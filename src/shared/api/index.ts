/**
 * Public API client exports.
 *
 * @example
 * import { get, post, patch, del, apiFetch, ApiError } from "@/shared/api";
 */
export {
  apiFetch,
  get,
  post,
  patch,
  del,
  ApiError,
  type ApiFetchOptions,
} from "./apiClient";
